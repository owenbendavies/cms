sudo: false

branches:
  only: master

language: ruby

cache: bundler

services:
  - redis-server

env:
  - DATABASE_URL=postgres://postgres@localhost/cms

before_script:
  - ./bin/bootstrap

script:
  - ./bin/test
  - git clean --force public/assets/
  - git checkout public/assets/
  - ./bin/changes
