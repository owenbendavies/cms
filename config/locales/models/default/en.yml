---
en:
  activerecord:
    errors:
      messages:
        contains_html: html is not allowed
        invalid_email_address: is not a valid email address
        invalid_json: is not valid
        weak_password: "%{feedback}"
