<% content_for :title, @page.name %>
<% content_for :page_icon, t('.private.icon') if @page.private %>

<%= @page.html_content&.html_safe %>

<%= @page.custom_html&.html_safe %>

<%= render 'pages/contact_form', message: @message, site: @site if @page.contact_form %>
