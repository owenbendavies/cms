- content_for :title, t('.title')
- content_for :page_icon, t('.icon')

%table.table.table-bordered
  %thead
    %tr
      %th= User.human_attribute_name(:email)

  %tbody
    - @site.all_users.each do |user|
      %tr
        %td= user.email
