- content_for :title, t('.title')
- content_for :body_id, 'page_url_login'

= simple_form_for User.new, url: login_path do |form|
  = form.input :email, autofocus: true
  = form.input :password

  .form-group
    .col-control-offset
      = form.button :submit, value: t('.title')
