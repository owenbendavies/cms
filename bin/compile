#!/bin/bash
set -e
cd `dirname $0`/..

export DISABLE_SPRING='true'
export RAILS_ENV='production'
export RAILS_GROUPS='assets'
export SECRET_KEY_BASE='xxxxxxxxxxxxxxxxxxxx'

set -v

./bin/rake assets:precompile assets:clean[0]

# 'Compile complete'
