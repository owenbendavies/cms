{
  "name": "obduk C.M.S.",
  "description": "A Content Management System (CMS) written with Ruby on Rails",
  "repository": "https://github.com/obduk/cms",
  "addons": [
    "heroku-postgresql",
    "sendgrid"
  ],
  "env": {
    "SYSADMIN_EMAIL": {
      "description": "The email address for the first system administrator account"
    },
    "EMAIL_FROM_ADDRESS": {
      "description": "Email address to use for messages"
    },
    "HEROKU_APP_NAME": {
      "description": "Set to same as the Heroku App name (set automatically for review apps)"
    },
    "AWS_REGION": {
      "description": "The AWS region used for S3 and optionally SES",
      "value": "us-east-1"
    },
    "IAM_KEY": {
      "description": "The Amazon IAM user key"
    },
    "IAM_SECRET": {
      "description": "The Amazon IAM user secret"
    },
    "S3_BUCKET": {
      "description": "The name of the Amazon S3 bucket"
    },
    "S3_HOST": {
      "description": "The host of the Amazon S3 bucket or CloudFront endpoint for the bucket (defaults to https://S3_BUCKET.s3-AWS_REGION.amazonaws.com)",
      "required": false
    },
    "ASSET_HOST": {
      "description": "Sets the rails asset host",
      "required": false
    },
    "HOST": {
      "description": "The host for the first site (defaults to HEROKU_APP_NAME.herokuapp.com)",
      "required": false
    },
    "SECRET_KEY_BASE": {
      "description": "A randomly generated secret to verify sessions",
      "generator": "secret"
    },
    "BUNDLE_WITHOUT": {
      "description": "Don't install asset gems on Heroku",
      "value": "development:test:assets"
    }
  },
  "scripts": {
    "postdeploy": "./bin/rake db:migrate db:seed"
  }
}
